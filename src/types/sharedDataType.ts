export interface SharedLink {
  id: number;
  createdAt: string;
  description: string;
  imageSource?: string;
  title: string;
  url: string;
}
export interface SharedInfo {
  ownerName: string;
  folderName: string;
  profileImage: string;
}
